apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.rabbitmq.name }}
  labels:
    {{- include "vprofile-app.labels" . | nindent 4 }}
    app: {{ .Values.rabbitmq.name }}
spec:
  type: {{ .Values.rabbitmq.service.type }}
  ports:
    - port: {{ .Values.rabbitmq.service.port }}
      targetPort: {{ printf "%s-port" .Values.rabbitmq.name }}
      protocol: TCP
      name: {{ .Values.rabbitmq.name }}
  selector:
    app: {{ .Values.rabbitmq.name }}